public enum Token {
    // Syntax symbols
    LPAREN,
    RPAREN,
    QUOTE,

    // Literals
    NUM_LITERAL,
    STR_LITERAL,
    BOOL_LITERAL,

    // Keywords
    KEYWORD,

    // Others
    NAME,
    EOF
}
